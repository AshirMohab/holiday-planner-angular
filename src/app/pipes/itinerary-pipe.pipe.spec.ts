import { ItineraryPipePipe } from './itinerary-pipe.pipe';

describe('ItineraryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ItineraryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
